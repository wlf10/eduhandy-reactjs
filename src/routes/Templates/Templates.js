import React, { Fragment } from 'react';

import Typography from '@material-ui/core/Typography';

const Templates = ({classes}) => { 
  return (
    <Fragment>
      <Typography paragraph>
          Тут будут шаблоны распсианий
      </Typography>
    </Fragment>
  );
}

export default Templates;
